# Frontend configuration
REACT_APP_API_URL=http://localhost:5000

# Backend configuration
# FLASK_ENV=development
# FLASK_DEBUG=True

# LaTeX compilation mode: 'api' (serverless-friendly) or 'local' (requires TeX Live)
# Default is 'api' for serverless deployment
LATEX_COMPILE_MODE=api

# Optional: Custom LaTeX API URL (default: https://latex.ytotech.com/builds/sync)
# LATEX_API_URL=https://latex.ytotech.com/builds/sync
